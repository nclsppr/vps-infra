# TYPE  DATABASE       USER                    ADDRESS          METHOD
local   all            all                                      trust
host    all            all                     127.0.0.1/32     trust
host    all            all                     ::1/128          trust

# These CIDRs are owned by Ansible. The external Docker networks must be
# created with these exact subnets before the platform is started.
host    surplasse      surplasse_runtime       172.30.11.0/24   scram-sha-256
host    surplasse      surplasse_migrator      172.30.11.0/24   scram-sha-256
host    parkventory    parkventory_runtime     172.30.21.0/24   scram-sha-256
host    parkventory    parkventory_migrator    172.30.21.0/24   scram-sha-256
host    all            postgres_exporter       172.30.31.0/24   scram-sha-256

host    all            all                     0.0.0.0/0        reject
host    all            all                     ::0/0            reject
