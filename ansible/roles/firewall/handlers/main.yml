---
- name: Reload systemd units
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Apply Docker ingress firewall
  ansible.builtin.service:
    name: vps-docker-ingress-firewall.service
    state: restarted
