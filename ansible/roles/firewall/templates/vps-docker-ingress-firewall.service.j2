[Unit]
Description=Restrict Docker published ports to the VPS public ingress contract
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/vps-docker-ingress-firewall
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
