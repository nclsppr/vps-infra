#!/bin/sh
set -eu

if ! /usr/bin/grep -Fq /atlas-codex.slice/ /proc/self/cgroup; then
  printf '%s\n' 'Codex must run through sudo atlas-codex' >&2
  exit 77
fi

exec {{ vps_codex_install_root }}/current/bin/{{ vps_codex_entrypoint }} "$@"
