---
extends: default

rules:
  document-start:
    present: true
  line-length:
    max: 100
    level: warning
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: false
  comments:
    min-spaces-from-content: 1

ignore: |
  .venv/
  docs/archive/
